<?php
if ( ! defined( 'ABSPATH' ) ) {
    exit; // Exit if accessed directly
}

class WPaigen_Settings {
    public function __construct() {
        // Add settings page if needed, for now settings are handled via options API
    }
}